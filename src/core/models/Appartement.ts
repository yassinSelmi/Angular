export class Apartment {
    apartNum!: number; // Apartment number
    floorNum!: number; // Floor number
    surface!: number; // Surface area in square meters
    terrace!: boolean; // Indicates if there is a terrace
    surfaceTerrace!: number; // Surface area of the terrace in square meters
    category!: string; // Category of the apartment (e.g., Studio, 1-Bedroom)
    residenceId!: number; // Associated Residence ID
  }  